Type meldinger:
delete order: 1 ordre skal slettes
 Header: ordren som skal slettes. 
 Body: hele ordrelista
 
 Hva skjer: 1. Alle sletter ordren fra lista. 
			2. Kjører union på resten.

new order 
 Header: ordren som skal legges til. 
 Body: hele ordrelista
 
 Hva skjer: 1. Legger til ordre i lista
			2. Kjører union på resten.

update:
	if (status == delete)
		pop
	if (status == new)
		push
	
	if (union_list(my_list, other_list) == ERROR)
		send RECAP

